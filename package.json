{
  "name": "heimdahl-js",
  "version": "1.0.0",
  "description": "JavaScript SDK for the Heimdahl blockchain data platform",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "example": "node examples/usage.js",
    "build": "esbuild src/index.js --bundle --outfile=dist/heimdahl.js",
    "build:min": "esbuild src/index.js --bundle --minify --outfile=dist/heidmahl.min.js",
    "build:all": "npm run build && npm run build:min",
    "demo": ""
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "arbitrum",
    "crypto",
    "api",
    "sdk"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.25.2",
    "express": "^4.21.2",
    "node-fetch": "^2.6.7"
  },
  "engines": {
    "node": ">=12.0.0"
  },
  "browser": {
    "node-fetch": false
  }
}
